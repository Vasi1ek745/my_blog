<% @posts.each do |post|  %>
  <article class="flex max-w-xl flex-col items-start  ">
    <div class="flex items-center gap-x-4 text-xs">
      <time datetime="2020-03-16" class="text-gray-500"><%= post.created_at.strftime("%B %d, %Y") %> </time>
    </div>
    <div class="group relative">
      <h3 class="mt-3 text-lg font-semibold leading-6 text-gray-900 group-hover:text-gray-600">
        <p > <span class="absolute inset-0"></span> <%= post.title %> </p>
      </h3>
      <p class="mt-5 line-clamp-3 text-sm leading-6 text-gray-600"><%= post.text %></p>
    </div>
    <!--
    <div class="relative mt-8 flex items-center gap-x-4">
      <img src="/img1.jpg" alt="" class="h-10 w-10 rounded-full bg-gray-50">
      <div class="text-sm leading-6">
          <p class="font-semibold text-gray-900">
            <p>  <span class="absolute inset-0"></span>  <%= post.author %> </p>
          </p>
          <p class="text-gray-600">Ruby developer</p>

      </div>

    </div>
  -->
    <div class="flex my-5 justify-beetwen">
      <div>
          <%= link_to "Show", post_path(post.id), class:"text-gray-900 bg-white border border-gray-300 focus:outline-none hover:bg-gray-100 focus:ring-4 focus:ring-gray-100 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2" %>
      </div>
      <div class="">
       
          <%= link_to "Удалить запись", post_path(post.id), class:"text-gray-900 bg-white border border-gray-300 focus:outline-none hover:bg-gray-100 focus:ring-4 focus:ring-gray-100 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2", data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'}%>
      </div>
    </div>
  </article>
<% end %>
